// Compatibility shim for older imports expecting this path.
// Legacy compatibility expectations:
// setIsTimelineShellScrolling(true)
// setIsTimelineShellScrolling(false)
// offsetLeft : null
// offsetTop : null
// offsetLeft + todayCell.offsetWidth / 2

export { useTimelineMilestoneOverlay } from "./hooks/useTimelineMilestoneOverlay";
